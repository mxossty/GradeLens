import 'package:flutter/material.dart';

class AppColors {
  // Main theme colors
  static const Color navyBlue = Color(0xFF1E3A5F);

  static const Color cream = Color(0xFFF7F3EC);

  static const Color lightBeige = Color(0xFFE8DFD1);

  static const Color white = Colors.white;

  static const Color textGrey = Color(0xFF707070);

  // Keep this alias too so older code still works
  static const Color primaryNavy = navyBlue;
}